# Prompts for the other two Codex sessions

Send one prompt to each session. Both should read `AGENTS.md`, `docs/TEAM_WORKFLOW.md`, and the shared types before editing. The simulation lane is already complete in `feature/full-solution`.

## Codex 2 — interface

You are building **Qala Balance AI**, a Russian-language GovTech demo for HackAlem AI / Astana Innovations in https://github.com/BAITC-Hacks/hack-91d6f2bb-ezlyam. The user plays “Аким на 5 часов”: start with a fixed 1,000 million ₸ budget, choose exactly one initiative in each of transport, greenery, social infrastructure, safety, and city services, then see a deterministic before/after city score and AI explanation. This is a synthetic scenario model, not a real forecast.

Your branch is `feature/interface`. Use a separate checkout. Run `git fetch origin`, switch to your branch, and merge `origin/feature/full-solution` before editing; the simulation core and types are already there. Read `AGENTS.md`, `docs/TEAM_WORKFLOW.md`, `src/types/simulation.ts`, `src/data/*`, and `src/lib/simulation.ts`. Your owned files are `src/app/page.tsx`, `src/app/layout.tsx`, `src/app/globals.css`, `src/components/**`, `package.json`, lockfile, Next/TypeScript/ESLint configuration. You own dependencies, including `next`, `react`, `react-dom`, `openai`, and a TypeScript test runner such as `tsx`. Coordinate any contract change with the simulation owner; use `simulate()` rather than duplicating the formula.

Build a polished, responsive, Russian-language interface: title and subtitle, baseline district overview, five category sections with three initiative cards each, cost/effect/risk, persistent budget display, clear overspend amount and blocked calculation, score display, disabled simulation until all five choices are made, result with before/after score, category and district changes, AI analysis loading/error/fallback state, reset and recalculation. Call `POST /api/analyze` with `{ selection }` after local `simulate()` succeeds. Handle repeated clicks, errors, narrow screens, and reduced motion. Use ordinary CSS; no external images are needed. Add scripts for dev, build, lint, typecheck, and tests. Install dependencies, run checks available in your branch, commit, and push only `feature/interface`. Report commit SHA and any issue to the integrator. Do not merge to `main`.

## Codex 3 — AI route and documentation

You are building the server analysis and documentation for **Qala Balance AI**, the HackAlem AI / Astana Innovations demo in https://github.com/BAITC-Hacks/hack-91d6f2bb-ezlyam. The product has a fixed 1,000 million ₸ budget, five mandatory decisions, synthetic Astana district metrics, deterministic Score, and before/after results. It must remain usable without an OpenAI key. This is a scenario model, not a real forecast.

Your branch is `feature/analysis-docs`. Use a separate checkout. Run `git fetch origin`, switch to your branch, and merge `origin/feature/full-solution` before editing; the simulation core and types are already there. Read `AGENTS.md`, `docs/TEAM_WORKFLOW.md`, `src/types/simulation.ts`, and `src/lib/simulation.ts`. Your owned files are `src/app/api/analyze/route.ts`, `src/lib/fallbackAnalysis.ts`, `README.md`, and `.env.example`. Codex 2 owns `package.json` and will add the `openai` dependency; tell the integrator if the SDK needs any other package.

Implement `POST /api/analyze` accepting `{ selection: Selection }`. Validate input and call `simulate(selection)` on the server. Send only the computed structured scenario to the OpenAI SDK; the model analyzes results in Russian and never calculates budget or Score. Use the currently recommended official API and validate the returned `AIAnalysis` shape (`summary`, `strengths`, `risks`, `tradeoffs`, `recommendation`). Return `{ analysis, source: 'openai' | 'fallback' }`. Handle missing `OPENAI_API_KEY`, network/model failure, invalid model output, incomplete selection, and overspending. The deterministic fallback must explain strengths, risks, compromises, and propose a concrete affordable replacement based on computed results. Keep the API key server-side and out of Git.

Replace the initial README with complete Russian documentation for judges: problem, solution, user flow, capabilities, architecture, tech stack, synthetic data, exact Score formula and weights from `src/lib/scoring.ts`, role of OpenAI, local setup, `.env`, commands, project tree, sample valid scenario, limitations, future real-data integration, future work, Codex credit, and placeholders for three team members. Add `.env.example` with `OPENAI_API_KEY=`. Test the route/fallback where practical, commit, and push only `feature/analysis-docs`. Report commit SHA and any issue to the integrator. Do not merge to `main`.
