# Проверка версии — 23 сентября 2026

Проверена локальная рабочая копия, первоначально полученная из архива проекта. Перед подготовкой коммита она связана с историей `origin/main` в отдельной ветке `fix/dataset-compliance`. Исходный архив сохранён.

## Выполнено

- Установка по lockfile: pnpm 11.25.0, Node.js 24.19.0; exit 0.
- 33 теста модели/API: 33 passed, 0 failed.
- TypeScript `tsc --noEmit`: exit 0.
- ESLint `eslint src scripts`: exit 0.
- `pnpm check:example`: exit 0; стоимость 95, база 52.55768, Score 56.54307.
- `pnpm build`: exit 0, Next.js 15.5.26; главная страница и серверный `/api/analyze` собраны.
- Проверка пробелов сравнением с исходными файлами через `git diff --no-index --check`: ошибок пробелов нет.
- На момент проверки production-сервер успешно запущен на http://127.0.0.1:3000. Для повторного запуска используйте README.

## Проверено в браузере

- Исходное состояние: 0/5, расход 0, Score сценария отсутствует, AI-кнопка недоступна.
- Кнопка примера загружает 5 мер, расход 95, Score 56,54.
- Запрос объяснения показывает «локальный анализ (без LLM)».
- Перенос M10 из Нуры в Есиль меняет Score на 56,26 и сбрасывает прошлое объяснение.
- M4 + M7 в Нуре дают причину конфликта, Score отсутствует, AI-кнопка заблокирована.
- Набор стоимостью 101 даёт превышение на 1, Score отсутствует, AI-кнопка заблокирована.
- Сброс удаляет выбор и результат, возвращает бюджет 100.
- Ошибок и предупреждений в журнале браузера на проверенном пути нет.

## Покрытие математических проверок

В тестах сверены все 14 мер и 50 исходных показателей с PDF, веса, население, бюджет 100/101, ровно пять решений, уникальность, лимит направления, область мер, несовместимости, все синергии, четыре лага, отрицательный эффект M11, порог <40, отсутствие ограничения самого Score, применение clip после суммы, неизменность данных и 120 перестановок контрольного набора. Все 2002 сочетания пяти уникальных ID проверены с одним детерминированным назначением районов на сочетание и независимым расчётом. Это не исчерпывающая проверка всех назначений районов.

## Осталось перед подачей

1. Настроить личный OpenAI API-ключ локально и проверить ответ `source: openai`. Сейчас выполнены только проверки имитацией и реальный локальный fallback; внешнего вызова не было.
2. При необходимости уточнить роли команды в README; имена сохранены из актуального `main`.
3. Проверить PR с исправлениями перед объединением с `main`.
4. Уточнить инструкции организаторов: дедлайн, формат, доступ к репозиторию, видео и необходимость размещения сайта. Подача не выполнялась.
